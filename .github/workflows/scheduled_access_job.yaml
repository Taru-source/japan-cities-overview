name: Scheduled URL Access

on:
  schedule:
    - cron: '0 */12 * * *'

jobs:
  curl-job:
    runs-on: ubuntu-latest

    steps:
      - name: Curl the URL
        run: curl -I https://japan-cities-overview.vercel.app/
